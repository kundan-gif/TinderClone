<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loginpage">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoginPage">
        <activity android:name=".BoostActivity"></activity>
        <activity android:name=".TinderPlus" />
        <activity android:name=".wlcmtotinder" />
        <activity android:name=".GoogleLogInPage" />
        <activity android:name=".FacebookLogin" />
        <activity android:name=".MobileNumberPage" />
        <activity android:name=".accountpage" />
        <activity android:name=".messegepage" />
        <activity android:name=".likespage" />
        <activity android:name=".homepage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>